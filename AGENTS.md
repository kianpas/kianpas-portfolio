# AGENTS.md

## Project
Developer blog + portfolio site.
Preserve existing routes, slugs, and post formatting.

## Mandatory rules
- Do not rename or change existing page paths.
- Do not modify frontmatter keys.
- Avoid broad refactors unless explicitly requested.
- Do not add new dependencies without clear necessity.

## Run / Verify
Use the existing package manager defined in the repo.
Primary commands are defined in package.json.

## Non-standard constraints
(Only include if applicable)
- Static-first rendering strategy must be preserved.
- Content source directory must remain unchanged.

## Change output format
When proposing changes:
- List modified files
- Explain verification steps